// Defines the config object this component expects, but also serves as the
// defaults for testing this component in isolation
// JS format is used (instead of JSON) to allow comments, etc.
var spec = {
  tweet: {
    url: 'http://www.massrelevance.com/',
    text: 'Super Cool & Neat!',
    related: 'massrelevance',
    hashtags: 'test',
    button_text: 'Tweet'
  },
  follow: {
    screen_name: 'jryans'
  }
}
